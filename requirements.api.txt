# ─────────────────────────────────────────────────────────────────────────────
# requirements.api.txt — P2D API Container (Cloud Run)
# Contexto: DeepSeek primário | OpenAI embeddings obrigatório | Gemini DESCARTADO
#
# Diferenças vs requirements.txt:
#   REMOVIDO: streamlit, plotly, pyvis, streamlit-javascript, openpyxl
#             google-generativeai, google-genai  (Gemini free tier descartado)
#   ADICIONADO: google-cloud-secret-manager, google-cloud-tasks
# ─────────────────────────────────────────────────────────────────────────────

# ── LLM providers (DeepSeek via openai SDK; OpenAI obrigatório p/ embeddings) ─
anthropic==0.49.0
openai==1.65.0
pydantic==2.12.5

# ── NLP ───────────────────────────────────────────────────────────────────────
spacy>=3.7,<4.0
# Modelo pt após instalar: python -m spacy download pt_core_news_lg

# ── Exportação / Importação de documentos ─────────────────────────────────────
pypdf>=5.0.0
python-docx==1.1.2
fpdf2==2.8.2
python-pptx==1.0.2

# ── Validação ─────────────────────────────────────────────────────────────────
jsonschema>=4.21
lxml>=5.0

# ── Utilitários ───────────────────────────────────────────────────────────────
python-dateutil>=2.9
json-repair==0.60.1

# ── Orquestração ──────────────────────────────────────────────────────────────
langgraph==1.2.0

# ── Supabase ──────────────────────────────────────────────────────────────────
supabase>=2.4.0

# ── API FastAPI ────────────────────────────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.49.0
python-multipart==0.0.9
httpx==0.28.1

# ── Google Cloud (Secret Manager 4 camadas + Cloud Tasks fila de pipeline) ─────
google-cloud-secret-manager==2.22.0
google-cloud-tasks==2.16.3

# ── Testes ────────────────────────────────────────────────────────────────────
pytest>=8.0
